disko
WIP -> sops/agenix
nur
impermanence
WIP -> home-manager/hjem

mount /boot/efi/EFI to /boot/EFI directly
DONE -> fix suspend
limine bootloader ?
grub-btrfs (works on nixos?)
snapper/timeshift (btrfs)
DONE -> network shares (smb)
symlink data folders (dl,doc,mus,vid,dsk,prj)
DONE -> dwmblocksasync
  - Needs visual fix for missing icon before modules
fix bluetooth audio (LG XBOOM only)
user directories
DONE -> background (feh) (TODO -> refine it)
DONE -> printing (cups+avahi)
podman
WIP -> devshells
  - DONE -> python
  - C
  - Rust

specialization for each kernel (switch to the wanted one using grub)
cachyos_kernel-lto

try xlibre

WIP -> dwm (+ patches)
oxwm
qtile

lightdm

lxsession?

add gtk/qt looks
stylix

helium widevine fix
lf/yazi
fzf
nvim config (lsp, treesitter, completions, multiple cursors, markdown previews, fzf)
tmux/zellij
xephyr (for testing wm)
winboat (garmin express)

DONE -> check multimonitor
DONE -> check external monitor

DONE -> make betterlockscreen/slock work
  - Need to add cache generation for the first time (taking all screens into account)
make picom work
DONE -> make autorandr work
  - DONE -> fix first boot (wrong resolution)
DONE -> make redshift work

WIP -> home-manager/hjem config files + some services?
WIP -> better flake modularization + reusable modules (need to work on modules shared by multiple systems, 1 system for now)
  - WIP -> Nvidia:
    - DONE -> Nvidia modular config for all hosts
    - DONE -> Nvidia modular config toggle
    - DONE -> Nvidia modular driver package injection
    - DONE -> Nvidia modular prime (offload/sync) + specialization
    - Separate Nvidia and Intel configs
